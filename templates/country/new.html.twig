{% extends 'base.html.twig' %}

{% block title %}New Country{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Country</h1>

        {{ include('country/_form.html.twig') }}

        <a href="{{ path('app_country_index') }}" class="btn btn-secondary mt-3">Back to list</a>
    </div>
{% endblock %}

